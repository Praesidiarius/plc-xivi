# Twig is a dev/test-only dependency; see the note in config/bundles.php.
# Configuring it outside those environments would fail to load in production.
when@dev:
    twig:
        file_name_pattern: '*.twig'

when@test:
    twig:
        file_name_pattern: '*.twig'
        strict_variables: true
