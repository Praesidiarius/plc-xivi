{# No dismiss buttons: they need Bootstrap's JavaScript, which this app does not ship. #}
{% for type, messages in app.flashes(['success', 'error']) %}
	{% for message in messages %}
		<div class="alert alert-{{ type == 'error' ? 'danger' : 'success' }}" role="status">{{ message }}</div>
	{% endfor %}
{% endfor %}
